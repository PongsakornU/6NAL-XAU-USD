name: XAUUSD Swing Signals

on:
  # Triggered externally by cron-job.org (punctual) via the workflow_dispatch
  # REST endpoint. The "test" toggle is also usable from the Actions UI button.
  workflow_dispatch:
    inputs:
      test:
        description: 'Send a one-time test ping (ignores conditions)'
        type: boolean
        default: false

permissions:
  contents: write          # required so the run can commit the state file back

jobs:
  signal:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run signal check
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          TD_KEY: ${{ secrets.TD_KEY }}
          TEST_MODE: ${{ inputs.test }}
        run: python xauusd_discord_signals.py

      - name: Run trade tracker
        # Runs right after the signal check in the same job (zero delay, same
        # market data). Picks up any fresh trade and pings the tracking channel.
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          TRACKER_WEBHOOK: ${{ secrets.TRACKER_WEBHOOK }}
          TD_KEY: ${{ secrets.TD_KEY }}
          TEST_MODE: ${{ inputs.test }}
        run: python xauusd_trade_tracker.py

      - name: Persist state (signal de-dupe + trade tracking memory)
        if: always()   # still save signal state even if the tracker step errors
        run: |
          if [[ -n "$(git status --porcelain xau_signal_state.json xau_trade_tracker.json)" ]]; then
            git config user.name  "github-actions"
            git config user.email "actions@github.com"
            git add xau_signal_state.json xau_trade_tracker.json
            git commit -m "update signal state [skip ci]"
            git push
          else
            echo "No state change."
          fi